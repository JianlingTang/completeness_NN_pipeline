# Minimal deps for CI and NN step (no IRAF/PyRAF)
numpy>=1.20
scipy>=1.7
pandas>=1.3
pyarrow>=6.0
matplotlib>=3.4
astropy>=5.0
pytest>=7.0
# For NN training (optional for pipeline-only runs)
torch>=1.10
scikit-learn>=1.0
joblib>=1.0
# For completeness API (optional)
fastapi>=0.100
uvicorn[standard]>=0.22
